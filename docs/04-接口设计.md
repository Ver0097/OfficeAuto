# OA管理系统 - 接口设计

## 1. 接口规范

### 1.1 统一响应格式

```java
public class Result<T> {
    private Integer code;       // 状态码：200成功，其他失败
    private String message;     // 提示消息
    private T data;             // 业务数据
    private Long timestamp;     // 时间戳

    // 成功响应
    public static <T> Result<T> success(T data) {
        return new Result<>(200, "操作成功", data);
    }

    // 失败响应
    public static <T> Result<T> error(Integer code, String message) {
        return new Result<>(code, message, null);
    }
}
```

### 1.2 分页请求格式

```java
public class PageRequest {
    private Integer pageNum = 1;     // 页码
    private Integer pageSize = 10;   // 每页数量
    private String orderBy;          // 排序字段
    private String orderType;        // 排序方式：asc/desc
}
```

### 1.3 分页响应格式

```java
public class PageResult<T> {
    private Long total;              // 总记录数
    private Integer pages;           // 总页数
    private Integer pageNum;         // 当前页码
    private Integer pageSize;        // 每页数量
    private List<T> list;            // 数据列表
}
```

---

## 2. 认证接口

### 2.1 登录

```
POST /api/auth/login

请求体：
{
    "username": "admin",
    "password": "123456"
}

响应：
{
    "code": 200,
    "message": "登录成功",
    "data": {
        "token": "eyJhbGciOiJIUzI1NiIs...",
        "userInfo": {
            "id": 1,
            "username": "admin",
            "realName": "管理员",
            "avatar": "xxx",
            "deptId": 1,
            "roles": ["admin"]
        }
    }
}
```

### 2.2 登出

```
POST /api/auth/logout

响应：
{
    "code": 200,
    "message": "登出成功"
}
```

### 2.3 获取用户菜单

```
GET /api/auth/menu

响应：
{
    "code": 200,
    "data": [
        {
            "id": 1,
            "name": "系统管理",
            "path": "/system",
            "icon": "setting",
            "children": [
                {
                    "id": 2,
                    "name": "用户管理",
                    "path": "/system/user",
                    "component": "system/user/index",
                    "perms": "system:user:list"
                }
            ]
        }
    ]
}
```

---

## 3. 用户管理接口

### 3.1 用户列表（分页）

```
GET /api/system/user/list?pageNum=1&pageSize=10&username=&status=

响应：
{
    "code": 200,
    "data": {
        "total": 100,
        "pages": 10,
        "pageNum": 1,
        "pageSize": 10,
        "list": [
            {
                "id": 1,
                "username": "admin",
                "realName": "管理员",
                "email": "admin@example.com",
                "phone": "13800138000",
                "deptId": 1,
                "deptName": "技术部",
                "status": 1,
                "createTime": "2024-01-01 10:00:00"
            }
        ]
    }
}
```

### 3.2 用户详情

```
GET /api/system/user/{id}

响应：
{
    "code": 200,
    "data": {
        "id": 1,
        "username": "admin",
        "realName": "管理员",
        "email": "admin@example.com",
        "phone": "13800138000",
        "deptId": 1,
        "status": 1,
        "avatar": "xxx",
        "roleIds": [1, 2]
    }
}
```

### 3.3 新增用户

```
POST /api/system/user

请求体：
{
    "username": "newuser",
    "password": "123456",
    "realName": "新用户",
    "email": "new@example.com",
    "phone": "13900139000",
    "deptId": 2,
    "roleIds": [3]
}

响应：
{
    "code": 200,
    "message": "添加成功"
}
```

### 3.4 编辑用户

```
PUT /api/system/user

请求体：
{
    "id": 2,
    "realName": "修改名称",
    "email": "new@example.com",
    "phone": "13900139000",
    "deptId": 3,
    "roleIds": [3, 4]
}

响应：
{
    "code": 200,
    "message": "修改成功"
}
```

### 3.5 删除用户

```
DELETE /api/system/user/{id}

响应：
{
    "code": 200,
    "message": "删除成功"
}
```

### 3.6 重置密码

```
PUT /api/system/user/resetPwd/{id}

请求体：
{
    "password": "newpassword"
}

响应：
{
    "code": 200,
    "message": "密码重置成功"
}
```

### 3.7 切换状态

```
PUT /api/system/user/status/{id}

请求体：
{
    "status": 0  // 0禁用 1启用
}

响应：
{
    "code": 200,
    "message": "状态修改成功"
}
```

---

## 4. 角色管理接口

### 4.1 角色列表

```
GET /api/system/role/list?pageNum=1&pageSize=10&name=&status=

响应：
{
    "code": 200,
    "data": {
        "total": 10,
        "list": [
            {
                "id": 1,
                "name": "超级管理员",
                "code": "admin",
                "description": "拥有所有权限",
                "status": 1,
                "createTime": "2024-01-01"
            }
        ]
    }
}
```

### 4.2 角色详情

```
GET /api/system/role/{id}

响应：
{
    "code": 200,
    "data": {
        "id": 1,
        "name": "超级管理员",
        "code": "admin",
        "description": "拥有所有权限",
        "status": 1,
        "menuIds": [1, 2, 3, 4, 5]
    }
}
```

### 4.3 新增角色

```
POST /api/system/role

请求体：
{
    "name": "部门经理",
    "code": "dept_manager",
    "description": "部门管理权限",
    "menuIds": [1, 2, 10, 11]
}

响应：
{
    "code": 200,
    "message": "添加成功"
}
```

### 4.4 编辑角色

```
PUT /api/system/role

请求体：
{
    "id": 3,
    "name": "部门经理",
    "description": "修改描述",
    "menuIds": [1, 2, 10, 11, 12]
}

响应：
{
    "code": 200,
    "message": "修改成功"
}
```

### 4.5 删除角色

```
DELETE /api/system/role/{id}

响应：
{
    "code": 200,
    "message": "删除成功"
}
```

### 4.6 分配菜单权限

```
PUT /api/system/role/menu/{roleId}

请求体：
{
    "menuIds": [1, 2, 3, 10, 11]
}

响应：
{
    "code": 200,
    "message": "权限分配成功"
}
```

---

## 5. 菜单管理接口

### 5.1 菜单树

```
GET /api/system/menu/tree

响应：
{
    "code": 200,
    "data": [
        {
            "id": 1,
            "name": "系统管理",
            "parentId": 0,
            "path": "/system",
            "type": 0,  // 目录
            "icon": "setting",
            "sort": 1,
            "children": [
                {
                    "id": 2,
                    "name": "用户管理",
                    "parentId": 1,
                    "path": "/system/user",
                    "component": "system/user/index",
                    "type": 1,  // 菜单
                    "perms": "system:user:list",
                    "sort": 1,
                    "children": [
                        {
                            "id": 100,
                            "name": "用户添加",
                            "parentId": 2,
                            "type": 2,  // 按钮
                            "perms": "system:user:add"
                        }
                    ]
                }
            ]
        }
    ]
}
```

### 5.2 新增菜单

```
POST /api/system/menu

请求体：
{
    "name": "用户添加",
    "parentId": 2,
    "type": 2,
    "perms": "system:user:add",
    "sort": 1
}

响应：
{
    "code": 200,
    "message": "添加成功"
}
```

### 5.3 编辑菜单

```
PUT /api/system/menu

请求体：
{
    "id": 100,
    "name": "用户添加",
    "perms": "system:user:add",
    "sort": 2
}

响应：
{
    "code": 200,
    "message": "修改成功"
}
```

### 5.4 删除菜单

```
DELETE /api/system/menu/{id}

响应：
{
    "code": 200,
    "message": "删除成功"
}
```

---

## 6. 部门管理接口

### 6.1 部门树

```
GET /api/system/dept/tree

响应：
{
    "code": 200,
    "data": [
        {
            "id": 1,
            "name": "总公司",
            "parentId": 0,
            "leader": "张总",
            "phone": "13800138000",
            "sort": 1,
            "children": [
                {
                    "id": 2,
                    "name": "技术部",
                    "parentId": 1,
                    "leader": "李经理",
                    "sort": 1
                }
            ]
        }
    ]
}
```

### 6.2 新增部门

```
POST /api/system/dept

请求体：
{
    "name": "研发组",
    "parentId": 2,
    "leader": "王组长",
    "phone": "13900139000",
    "sort": 1
}

响应：
{
    "code": 200,
    "message": "添加成功"
}
```

### 6.3 编辑部门

```
PUT /api/system/dept

请求体：
{
    "id": 3,
    "name": "研发组",
    "leader": "新组长",
    "sort": 2
}

响应：
{
    "code": 200,
    "message": "修改成功"
}
```

### 6.4 删除部门

```
DELETE /api/system/dept/{id}

响应：
{
    "code": 200,
    "message": "删除成功"
}
```

---

## 7. 审批接口

### 7.1 请假申请列表

```
GET /api/approval/leave/list?pageNum=1&pageSize=10&status=

响应：
{
    "code": 200,
    "data": {
        "total": 50,
        "list": [
            {
                "id": 1,
                "userId": 5,
                "userName": "张三",
                "type": 1,  // 事假
                "startDate": "2024-03-01",
                "endDate": "2024-03-03",
                "days": 3,
                "reason": "家中有事",
                "status": 0,
                "createTime": "2024-02-28 10:00:00"
            }
        ]
    }
}
```

### 7.2 提交请假申请

```
POST /api/approval/leave

请求体：
{
    "type": 1,
    "startDate": "2024-03-10",
    "endDate": "2024-03-12",
    "reason": "参加培训"
}

响应：
{
    "code": 200,
    "message": "申请提交成功"
}
```

### 7.3 审批请假

```
PUT /api/approval/leave/approve/{id}

请求体：
{
    "status": 1,  // 1通过 2拒绝
    "remark": "同意请假"
}

响应：
{
    "code": 200,
    "message": "审批成功"
}
```

### 7.4 报销申请列表

```
GET /api/approval/expense/list?pageNum=1&pageSize=10&status=

响应：
{
    "code": 200,
    "data": {
        "total": 30,
        "list": [
            {
                "id": 1,
                "userId": 5,
                "userName": "张三",
                "type": 1,  // 差旅
                "amount": 500.00,
                "description": "出差交通费",
                "status": 0
            }
        ]
    }
}
```

### 7.5 提交报销申请

```
POST /api/approval/expense

请求体：
{
    "type": 1,
    "amount": 500.00,
    "description": "出差交通费",
    "attachment": "/files/xxx.pdf"
}

响应：
{
    "code": 200,
    "message": "申请提交成功"
}
```

### 7.6 审批报销

```
PUT /api/approval/expense/approve/{id}

请求体：
{
    "status": 1,
    "remark": "同意报销"
}

响应：
{
    "code": 200,
    "message": "审批成功"
}
```

---

## 8. 接口状态码定义

| 状态码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未登录 |
| 403 | 无权限 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |
| 1001 | 用户名已存在 |
| 1002 | 密码错误 |
| 1003 | 用户被禁用 |
| 2001 | 角色编码已存在 |
| 3001 | 菜单名称已存在 |
| 4001 | 部门名称已存在 |
| 5001 | 审批状态错误 |